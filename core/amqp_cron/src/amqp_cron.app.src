{application, amqp_cron, [
    {description, "scheduled tasks execution"},
    {vsn, git},
    {modules, []},
    {registered, [amqp_cron_sup]},
    {applications, [
        kernel,
        stdlib,

        amqp_leader,

        lager
    ]},
    {mod, {amqp_cron_app, []}},
    {env, []}
]}.
